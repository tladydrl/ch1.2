# 부울(bool) 참이나 거짓을 나타내는 값
# True, False 두 리터럴(상수) 만 가진다.

a = 1  #
print(a > 1)  # 연산한 결과를 출력,, 어디다 저장하지는 않는다.

print(10 + 20)  # 이 결과를 사용하고 싶으면 어떤 변수에다가 담으면 된다.

print(a < 10)

b = a > 1
#print(b)  # True 아니면 False의 값을 가진다.

print(b, type(b))

print(b*10)  # 자바는 엄격, 이건 허용 x  # C 에서는 false는 0이고, true는 나머지.  # False -> 0 평가 (산술연산) ,, 여기서는  곱하기.

b = True
print(b*10)
print(True*False)  # 곱셈연산, 0과 1을 곱하게 된다.

# if( a > 1):  조건문 등에 많이 사용된다.
# while( a > 1):
# 컴퓨터는 0 과 1밖에 표현하지 못한다.

b = a > 1  # 값이 간다.,
print(b)
b = True  # 다른 저장공간을 가르키는 것이다. 변경 불가능.

## 정수값.
# 3.x 에서는 long이 사라졌다. int가 큰 값을.. 가진다..





































